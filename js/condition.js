// if > doc, write .. 
// else > clg 

let x = window.prompt("PLease First Valu")
let y = window.prompt(" Please Enter Second Value")
let z = window.prompt("Please Enter Sign")

if (z == "-"){
    document.write(x - y)
}
